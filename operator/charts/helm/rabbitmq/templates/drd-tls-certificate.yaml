{{- $install := and (eq (include "disasterRecovery.enableTls" .) "true") .Values.global.tls.generateCerts.enabled (not .Values.externalRabbitmq.enabled) (eq (include "rabbitmq.enableDisasterRecovery" .) "true") }}
{{- if and $install (eq (include "services.certProvider" .) "cert-manager") }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    {{- include "rabbitmq.defaultLabels" . | nindent 4 }}
  name: rabbitmq-drd-tls-certificate
  namespace: {{ .Release.Namespace }}
spec:
  secretName: {{ template "disasterRecovery.tlsSecretName" . }}
  duration: {{ default 365 .Values.global.tls.generateCerts.durationDays | mul 24 }}h
  commonName: rabbitmq-drd-ca
  isCA: false
  privateKey:
    rotationPolicy: Always
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  dnsNames:
{{ ( include "disasterRecovery.certDnsNames" . | indent 4) }}
  ipAddresses:
{{ ( include "disasterRecovery.certIpAddresses" . | indent 4) }}
  issuerRef:
  {{- if .Values.global.tls.generateCerts.clusterIssuerName }}
    name: {{ .Values.global.tls.generateCerts.clusterIssuerName }}
    kind: ClusterIssuer
  {{- else }}
    name: rabbitmq-services-tls-issuer
    kind: Issuer
  {{- end }}
    group: cert-manager.io
{{- end }}
